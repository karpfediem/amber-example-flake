import { exit } from "std/env"
import { len } from "std/text"

main(args) {
    if len(args) < 1 {
        echo "Please tell me your name!"
        exit(1)
    }
    let name = args[0]
    echo "Hello, {name}!"
}